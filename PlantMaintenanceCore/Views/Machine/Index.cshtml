@model PlantMaintenanceCore.Models.ViewModels.DisplayMachineViewModel
@{
    ViewData["Title"] = "Machine List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <div class="container">
        <h1>@ViewData["Title"]</h1>
        <a class="btn btn-info" href="/Machine/Edit/" style="float:right; margin-bottom:30px;">Add New</a>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Plant</th>
                    <th>Machine Name</th>
                    <th>Description</th>
                    <th hidden="">Status</th>
                    <th style="width: 150px">Action</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Plant</td>
                        <td>@item.MachineName</td>
                        <td>@item.Description</td>
                        <td hidden="">@(item.IsActive==true?"Active":"Passive")</td>
                        <td>
                            <a class="btn btn-success" href="/Machine/Edit/@item.Id/" style="float:right;">Edit</a>
                            <a class="btn btn-danger" href="/Machine/Delete/@item.Id/" style="float:right;">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

